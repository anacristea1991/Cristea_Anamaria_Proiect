@page
@model Cristea_Anamaria_Proiect.Pages.Counties.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>County</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="County.Id" />
            <div class="form-group">
                <label asp-for="County.Id" class="control-label"></label>
                <input asp-for="County.Id" class="form-control" readonly="readonly"/>
                <span asp-validation-for="County.Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="County.Name" class="control-label"></label>
                <input asp-for="County.Name" class="form-control" />
                <span asp-validation-for="County.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
